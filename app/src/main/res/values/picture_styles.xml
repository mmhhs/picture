<resources>

    <!-- Base application theme. -->
    <style name="AppThemePic" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppThemePic2" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
        <item name="android:colorForeground">@color/colorPrimary</item>
        <item name="android:colorBackground">@color/colorPrimary</item>
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="picture_background_white">
        <item name="android:background">@color/picture_white</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:clipToPadding">true</item>
    </style>
    <style name="picture_text_16">
        <item name="android:textColor">@color/picture_deep_grey</item>
        <item name="android:textSize">16sp</item>
    </style>
    <style name="picture_text_14">
        <item name="android:textColor">@color/picture_deep_grey</item>
        <item name="android:textSize">14sp</item>
    </style>
    <style name="picture_text_black_16">
        <item name="android:textColor">@color/picture_black</item>
        <item name="android:textSize">16sp</item>
    </style>
    <style name="picture_text_grey_deep_14">
        <item name="android:textColor">@color/picture_deep_grey</item>
        <item name="android:textSize">14sp</item>
    </style>
    <style name="picture_text_white_18">
        <item name="android:textColor">@color/picture_deep_grey</item>
        <item name="android:textSize">18sp</item>
    </style>

    <!-- listview style -->
    <style name="picture_style_listView">
        <item name="android:cacheColorHint">@android:color/transparent</item>
        <item name="android:divider">@color/picture_line_color</item>
        <item name="android:dividerHeight">0.4dp</item>
        <item name="android:listSelector">@android:color/transparent</item>
    </style>

    <!--位于中间dialog的风格-->
    <style name="PictureDialogCentre">
        <!--设置dialog的背景-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--设置Dialog的windowFrame框为无-->
        <item name="android:windowFrame">@null</item>
        <!--设置无标题-->
        <item name="android:windowNoTitle">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--设置窗口内容不覆盖-->
        <item name="android:windowContentOverlay">@null</item>
        <!--设置动画，在这里使用让它继承系统的Animation.Dialog-->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <!--背景是否模糊显示-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <!--底部dialog-->
    <style name="PictureActionSheetDialogStyle" parent="@android:style/Theme.Dialog">

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/PictureActionSheetDialogAnimation</item>
    </style>
    <!-- ActionSheet进出动画 -->
    <style name="PictureActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/picture_push_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/picture_push_bottom_out</item>
    </style>

    <style name="picture_background_grey">
        <item name="android:background">@color/picture_bg</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:clipToPadding">true</item>
    </style>

    <!-- 横向线条主题 -->
    <style name="picture_line_horizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dp_0_5</item>
        <item name="android:background">@color/picture_line_grey</item>
    </style>

    <style name="picture_line_horizontal_5">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dp_10</item>
        <item name="android:background">@color/picture_line_grey</item>
    </style>

</resources>
