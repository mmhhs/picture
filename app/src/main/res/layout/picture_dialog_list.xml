<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:id="@+id/popup_dialog_list_container_layout">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:background="@drawable/picture_bg_white3">

        <ListView
            android:id="@+id/popup_dialog_list_listview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:cacheColorHint="@android:color/transparent"
            android:divider="@color/picture_line_grey"
            android:dividerHeight="0.4dp"
            android:scrollbars="none"
            android:gravity="center"
            android:listSelector="@drawable/picture_selector_bg" />

        <View
            android:layout_width="match_parent"
            android:layout_height="@dimen/dp_10"
            android:background="@color/bg_grey"/>
        <TextView
            android:id="@+id/popup_dialog_list_cancel"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dp_50"
            android:gravity="center"
            android:background="@drawable/picture_selector_bg"
            android:text="@string/picture_cancel"
            android:textSize="16sp"
            android:textColor="@color/picture_black"/>
    </LinearLayout>
</LinearLayout>
